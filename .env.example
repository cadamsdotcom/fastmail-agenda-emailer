# =============================================================================
# Fastmail Daily Agenda — Configuration
# =============================================================================
# Copy this file to .env and fill in your values.
# The timezone is auto-detected from your Fastmail calendar settings.
#
# For GitHub Actions, set these as repository secrets instead.

# REQUIRED: Your Fastmail email address
FASTMAIL_USERNAME=you@fastmail.com

# REQUIRED: An App Password (NOT your main password)
# Generate one at: Fastmail → Settings → Privacy & Security → Integrations → App passwords
# Grant access to: CalDAV and SMTP
FASTMAIL_APP_PASSWORD=your-app-password-here

# OPTIONAL: Your name (used in the greeting, e.g. "Chris, here is your schedule")
# DISPLAY_NAME=Chris

# OPTIONAL: Send the agenda to a different address (default: same as username)
# SEND_TO=you@example.com

# OPTIONAL: Comma-separated list of calendar names to include (default: all)
# CALENDAR_NAMES=Personal,Work
